# mac version only for now
prefix: ./trieste
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - cartopy
  - tensorflow
  - pip
  - pip:
    - gpflow
    - trieste
    - hydra-core
    - -r dev-requirements.txt