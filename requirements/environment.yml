prefix: ./env
channels:
  - conda-forge
  - defaults
  - pytorch
dependencies:
  - python=3.8
  - cartopy
  - pytorch
  - torchvision
  - torchaudio
  - pip
  - pip:
    - gpytorch
    - hydra-core
    - -r dev-requirements.txt
